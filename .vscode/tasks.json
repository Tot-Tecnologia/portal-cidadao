{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "docker-compose-db",
        "type": "shell",
        "command": "docker-compose --env-file .env up -d",
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "problemMatcher": [],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      },
    ]
  }
  